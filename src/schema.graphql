type Post {
    id: ID!
    title: String
    content: String
    banner_url: String
    created_at: DateTime
    author: User
    tags: [String]
}

type User {
    id: ID!
    name: String
    posts: [Post]
}